[smtp]
listen=:25
host=mecca.local

[mailbox]
db.user=Gabriel
db.name=gomez
db.sslmode=disable
db.schema=schema/schema.sql

[agent]
# number of seconds to pause between sends
pause=60
# times to retry each MX host before giving up
mx.retry=2
# timeout in seconds for connection attempts
mx.timeout=5
# host to identify with at hello
hello=${host}

[test]
db.user=postgres
db.name=gomez_test
db.sslmode=disable
db.schema=schema/schema_test.sql
